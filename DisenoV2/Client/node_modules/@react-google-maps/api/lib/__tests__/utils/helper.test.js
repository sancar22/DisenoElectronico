"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var helper_1 = require("./../../utils/helper");
var updaterMap = {
    center: jest.fn(),
    radius: jest.fn()
};
describe("utils/helper", function () {
    describe('applyUpdaterToNextProps', function () {
        it('should invoke updater function if prop changed and not invoke if it hasent', function () {
            helper_1.applyUpdaterToNextProps(updaterMap, {}, { center: 1 }, {});
            expect(updaterMap.center).toBeCalledTimes(1);
            expect(updaterMap.radius).not.toBeCalled();
        });
    });
});
